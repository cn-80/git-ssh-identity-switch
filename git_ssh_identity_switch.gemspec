Gem::Specification.new do |s|
  s.name = 'git_ssh_identity_switch'
  s.version = '0.1.0'
  s.summary = 'Switch identity when using SSH to make changes to a git repository.'
  s.description = 'Switch identity when using SSH to make changes to a git repository.'
  s.authors = ['CN-80']
  s.email = 'cn80@localhost'
  s.files = ['lib/git_ssh_identity_switch.rb']
  s.homepage = 'https://github.com/cn-80/git-ssh-identity-switch'
  s.license = 'GPL+3.0'
  s.executables = ['switch-git']
end
